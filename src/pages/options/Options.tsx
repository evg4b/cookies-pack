import { Layout } from 'antd';
import React, { FC } from 'react';

const Options: FC = () => (
  <Layout>
    <div className="container">
      Options
    </div>
  </Layout>
);

export default Options;
